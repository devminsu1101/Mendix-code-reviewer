# L001·D003 개정 기록 (2026-08-06)

`npm run review` 결과를 실제 모델과 대조하다 나온 문제를 고친 기록.
무엇을 왜 바꿨는지가 남아 있어야 나중에 "이 등급이 왜 이렇지"를 다시 파헤치지 않는다.

---

## 1. 발단

`20260806_1004_revf3447284…md`에서 `common.VAL_IF_SABIS_002_AIR`가 L001로 🔴 1순위(점수 69)에 올랐다.
모델을 직접 열어보니 이랬다.

```
파라미터: Root(common.Root), httpRequest, httpResponse    ← 공개 서비스 common.Shipment_AIR
  Retrieve Data           ← assoc common.Data_Root            from $Root
  Retrieve InvoiceFiles   ← assoc common.InvoiceFiles_Data    from $Data
  Retrieve InvoiceFileList← assoc common.InvoiceFile_InvoiceFiles from $InvoiceFiles
  Loop (IterableList: $InvoiceFileList → IteratorAirShipmentCase)
    └ Retrieve ShipmentTracking.ShipmentCase
         ConstantRange / SingleObject                          ← "first" 단건
         XPath [HouseAwbNo_AIR = $Iterator/HouseBlNo or BookingNo = $Iterator/BookingNo]
    └ if $ShipmentCase = empty → 에러 JSON 생성 → Break
    └ else Continue
```

수신 payload의 각 건이 DB에 실존하는지 확인하는 **검증 루프**다.

## 2. 판정

### 탐지 자체는 정탐이다

`first`가 붙어도 쿼리 **횟수**는 반복 횟수 그대로다. N+1은 가져오는 행 수가 아니라 왕복 횟수 문제다.

| 비용 층 | `first`가 줄여주나 |
|---|---|
| ① 왕복 오버헤드 (네트워크·SQL 파싱·트랜잭션 컨텍스트) | ❌ 전혀 |
| ② DB 내부 탐색 (인덱스 O(log M) / 없으면 O(M)) | △ 인덱스 있을 때만 |
| ③ 결과 전송·객체화 | ⭕ 이것만 |

지배적인 비용은 ①이다. 그래서 "루프 밖으로 빼라"의 본질은 탐색을 줄이라는 게 아니라 **쿼리 횟수를 O(N) → O(1)로 만들라**는 것이다.

### 그러나 등급은 과대평가였다

1. **반복 횟수에 상한이 있다.** 루프 대상이 DB 리스트가 아니라 파라미터 `$Root`에서 3단계 연관 탐색으로 내려온 **메모리 리스트**다. 즉 반복 횟수 = 한 요청의 payload 건수. 테이블이 커져도 안 늘어난다.
2. **69점은 결함이 심해서가 아니다.** L001의 `baseScore`는 40으로 L005(50)·L002(45)보다 낮다. 공개 서비스 진입 ×1.8 배수 때문에 1위로 올라왔다.
3. **권장문구가 이 상황에 해롭다.** "루프 전에 리스트를 한 번에 Retrieve하라"를 문자 그대로 따르면 53속성 엔티티 전체를 메모리에 올리게 된다.

### 그리고 진짜 원인을 리포트가 못 짚고 있었다

`mprcontents` 전수 조사 결과 **이 모델에는 `DomainModels$Index` 정의가 0개다.**

```
총 Index 정의 수: 0
```

`ShipmentTracking.ShipmentCase`(속성 53개)도 인덱스가 없다. 따라서 저 XPath는 매 반복 풀스캔이고, **검증 로직 특성상 "못 찾는 경우"가 정상 동작**이라 `first`의 이점이 가장 안 나오는 경로(끝까지 스캔 후 empty 반환)를 일상적으로 밟는다.

기존 D003은 `속성 20개 초과 && 인덱스 0개`라는 대리 지표였다. 그래서 비영속 엔티티(`common.Root`, `Booking.NP_Booking` 등)까지 18건에 섞였고, **어느 속성에 인덱스를 걸라는 말을 못 했다.**

---

## 3. 변경 내용

### 신규 `src/analyzer/xpath.ts`

XPath 제약에서 조회 키를 뽑는 순수 함수. 모델 접근이 없어 자격증명 없이 테스트된다.

```ts
parseXPathKeys(xpath) → {
  directAttrs,        // 이 엔티티에 인덱스를 걸 수 있는 속성
  traversedPaths,     // 연관 경유 — 이 엔티티 인덱스로는 못 푼다
  topLevelOperator,   // and | or | single | none
  referencedVariables // 루프 이터레이터 참조 판정용
}
```

`topLevelOperator`가 필요한 이유: **`or`는 복합 인덱스가 쓰이지 않는다.** 각 조건이 독립적으로 인덱스를 타야 하므로 단일 컬럼 인덱스를 따로 만들어야 한다. 이 구분이 D003 권장문구를 가른다.

### L001 — 반복 상한으로 등급을 가른다

`scanFlowBody`가 루프 내 DB 조회를 구조체로 수집한다. 동시에 flow 전체를 훑어 **변수 출처 맵**을 만든다.

| 출처 | 판정 |
|---|---|
| `RetrieveAction` + `DatabaseRetrieveSource` | `db` — 행이 늘면 반복도 는다. 상한 없음 |
| `RetrieveAction` + `AssociationRetrieveSource` | `memory` — 이미 메모리에 있는 것 |
| `MicroflowParameterObject` | `param` |
| `CreateVariableAction` | `memory` |
| Java 액션·리스트 오퍼레이션 등 | `unknown` |

등급:

```
bounded = singleObject && usesIterator && loopDepth === 1
          && loopSourceOrigin ∈ {memory, param}
          && 변수 출처를 하나라도 해석했음

bounded → Warning, 점수 ×0.5
그 외   → Error,   점수 ×1.0
```

`scoreMultiplier`가 필요한 이유: `score = baseScore × 진입점 가중치`라서 severity만 낮추면 69점 그대로 상단에 남는다. 순위를 실제로 움직이려면 점수를 건드려야 한다.

**메시지 수정.** 기존 "DB 조회가 **1회** 발생합니다"의 1은 런타임 실행 횟수가 아니라 루프 안의 Retrieve 액션 개수였다. 읽는 사람은 "한 번뿐이네"로 오해한다.
→ `루프 안에 DB 조회 액션이 1개 있습니다 (반복마다 실행). 대상: …`

### D003 — 조회 키를 지목한다

`속성 20개 초과` 대리 지표를 버리고 실제 XPath 조회 키를 근거로 삼는다.

- 비영속 엔티티 제외
- 조회 조건에 안 쓰이는 엔티티 제외 (인덱스 얘기가 무의미)
- **leftmost prefix 반영** — 복합 인덱스의 2번째 이후 속성은 단독 조회를 못 받쳐주므로 `leadingAttrs`로만 커버 판정
- severity: 조회 키가 루프 안이거나 무인/외부 경로면 `Error`, 아니면 `Warning`
- score: `baseScore + 루프내사용×5 + hot flow수×3`

실제 출력(`Warehouse.Warehouse`): `조회 조건으로 쓰이는 속성 2개가 인덱스로 커버되지 않습니다: PartNo(3곳, 루프 내 2곳), HSCode(1곳)`

### 두 룰의 연결

L001 evidence에 그래프 계층에서 한 줄이 붙는다.

> `인덱스: ShipmentTracking.ShipmentCase에 인덱스 정의가 없습니다 — 조회 키 HouseAwbNo_AIR, BookingNo가 매 반복 풀스캔입니다. (D003 참조)`

`detectFlowIssues`는 **문서 하나만으로 재현 가능해야 한다**(blame이 이 계약에 의존). 그래서 인덱스 지식은 순수 판정에 넣지 않고 `analyzeFlow`에서만 붙인다.

### 리포트 신규 섹션 — `🔑 인덱스가 필요한 속성`

첫 실행에서 드러난 문제: **evidence는 TOP 10에만 렌더링된다.** 그런데 인덱스 문제는 개별 결함으로는 점수가 낮아 상단에 잘 안 올라온다. 실제로 첫 실행의 TOP 10은 L002 8건으로 채워졌고, L001(최고 52점)과 D003 ShipmentCase(51점)가 전부 밀려서 **교차 참조 라인이 한 줄도 출력되지 않았다.**

원인은 구조적이다. 로직 룰은 `baseScore × 진입점 가중치`를 타는데(L002는 페이지 진입만으로 59점), 도메인 룰은 `domain.ts`에서 점수를 직접 계산해 진입점 배수를 안 탄다.

순위를 손대는 대신 별도 섹션을 만들었다. 인덱스는 **한 엔티티에 모아 놓고 한 번에 처리하는 종류의 작업**이라, 순위와 별개로 통째로 보여주는 게 맞다.

```
### `ShipmentTracking.ShipmentCase` — 기존 인덱스 없음

| 속성 | 조회 | 루프 내 | 무인/외부 |
| businessRegistrationNumber | 3 | 3 | - |
| BookingNo | 3 | 2 | 2 |
...
> 조회 조건이 `or`로 묶여 있습니다. 복합 인덱스는 `or`에 쓰이지 않으므로
> 각 속성에 단일 컬럼 인덱스를 따로 만드세요.
```

D003이 지적한 엔티티만 대상으로 한다 — 룰과 다른 얘기를 하면 리포트가 신뢰를 잃는다.

---

## 4. Mendix 인덱스 참고

Studio Pro: 도메인 모델 → 엔티티 더블클릭 → **Indexes** 탭 → New → Add로 속성 추가 → 배포 시 실제 `CREATE INDEX`.

| 규칙 | 내용 |
|---|---|
| Persistable만 | 비영속 엔티티엔 Indexes 탭이 없다 |
| String 길이 | **Max length가 Unlimited면 인덱스 불가** (컨시스턴시 에러) |
| 자동 생성분 | PK(`id`)와 association 테이블 컬럼만. **일반 속성은 자동으로 안 만들어진다** |
| unique 아님 | Mendix 인덱스는 유일성 제약이 아니다. 유일성은 validation rule(앱 레벨) |
| 복합 인덱스 | leftmost prefix. `(A,B)`는 A 단독엔 쓰이지만 **B 단독엔 안 쓰인다** |
| `or` 조건 | 복합 인덱스가 안 먹는다. 단일 컬럼 인덱스를 각각 만들어야 한다 |
| 비용 | insert/update마다 인덱스도 갱신된다. 쓰기 많은 엔티티에 남발 금지 |

`VAL_IF_SABIS_002_AIR`에 필요한 것:

```
Index 1 : HouseAwbNo_AIR
Index 2 : BookingNo
```

`(HouseAwbNo_AIR, BookingNo)` 복합 인덱스 **하나로는 해결되지 않는다.**

---

## 5. 검증

```
npm test          selfcheck(SDK 심볼 21개) + blame 정확성
                  + XPath 파서 16케이스 + 인덱스 교차 참조 5케이스
                  — 전부 자격증명·네트워크 불필요, 1초
npm run review    실제 모델 대조 (약 2분 50초)
```

### 리포트에서 안 보이는 코드를 남기지 않기

L001의 인덱스 교차 참조 줄(`describeIndexCoverage`)은 evidence로만 나가고, evidence는 TOP 10에만 렌더링된다. 그런데 현재 모델에서 TOP 10은 L002 8건 + L005 2건으로 채워져서 **이 줄이 실제 리포트에 한 번도 출력되지 않았다.** 즉 눈으로 확인할 방법이 없었다.

순수 함수로 만들어 두었으므로 `src/xpath.test.ts`에서 합성 그래프로 직접 검증한다.

- 인덱스 0개 → 풀스캔 경고 + 조회 키 + D003 참조
- 복합 인덱스 `(BookingNo, X)` → leftmost prefix상 `BookingNo`만 커버로 인정, 미커버 키만 지목
- 전부 커버되면 그 사실을 적음
- 조회 키가 없거나 엔티티 정보가 없으면 **추측하지 않고 아무 줄도 만들지 않음**

리포트에서 안 보인다고 죽은 코드로 방치하면 나중에 조용히 틀린다. 버그 #0이 그렇게 생겼다.

## 6. 설계 원칙

- **모호하면 등급을 내리지 않는다(fail closed).** 변수 출처를 하나도 해석하지 못하면 `Error`를 유지하고 그 사실을 evidence에 적는다. 버그 #0(`instanceof undefined`가 삼켜져 전 룰이 0건)과 같은 조용한 실패를 만들지 않기 위해서다.
- **탐지는 결정론적 코드에, 해석은 리포트에.** 등급·점수는 관측된 사실의 함수여야 한다.
- **`detectFlowIssues`의 문서-로컬 계약 유지.** blame이 과거 커밋에서 같은 판정을 재현해야 한다.

## 7. 실측 (2026-08-06, `minsu-clean`, rev f3447284)

`20260806_1004`(변경 전) → `20260806_1442`(변경 후)

| 지표 | 이전 | 이후 |
|---|---:|---:|
| 전체 이슈 | 163건 | 161건 |
| 🔴 치명적 | 31건 | 29건 |
| L001 | 9건 | 9건 (등급만 변경) |
| D003 | 18건 | 16건 (내용 전면 교체) |
| 소요 시간 | — | 2분 51초 |

**L001 — 등급이 무차별로 내려가지 않았다.** 9건 중 4건만 하향.

| flow | 이전 | 이후 | 판정 근거 |
|---|---|---|---|
| `common.VAL_IF_SABIS_002_AIR` | 69 🔴 **1위** | **34 ⚠️** | payload 리스트 반복 + First + 이터레이터 키 |
| `common.VAL_IF_SABIS_002_SEA` | 69 🔴 2위 | 34 ⚠️ | 동일 |
| `Warehouse.ACT_RequestShipment_Save` | 🔴 | 26 ⚠️ | 동일 |
| `Warehouse.ACT_ShipmentStatus_ChangeApprove` | 🔴 | 26 ⚠️ | 동일 |
| `Adjustment.DS_Adjustment` | 🔴 | **52 🔴** | 상한 조건 미충족 |
| `CargoTracking.DS_CargoTracking` | 🔴 | **52 🔴** | 〃 |
| `ShipmentTracking.DS_ShipmentList_View` | 🔴 | **52 🔴** | 〃 |
| `BKSum.ACT_BKSumCopy1` / `Copy2` | 🔴 | **50 🔴** | 〃 |

`RetrieveAction.outputVariableName` 매핑 우려(§리스크)는 해소됐다. 값이 안 왔다면 4건 모두 Error로 남았을 것이다.

**D003 — 대리 지표에서 실제 조회 키로.**

```
이전: ShipmentTracking.ShipmentCase | 속성 53개인 대형 엔티티에 인덱스가 없습니다 | ⚠️ 15점
이후: ShipmentTracking.ShipmentCase | 조회 조건으로 쓰이는 속성 12개가 인덱스로 커버되지
      않습니다: businessRegistrationNumber(3곳, 루프 내 3곳), BookingNo(3곳, 루프 내 2곳,
      무인/외부 2곳), HouseAwbNo_AIR(2곳, 루프 내 1곳, 무인/외부 1곳), ... | 🔴 51점
```

비영속 엔티티(`common.Root`, `Booking.NP_Booking`, `QuotationRequest.NP_QuoteRequest`, `CargoTracking.Filter` 등)가 빠지고, 실제로 조회되는 `Warehouse.Warehouse`(PartNo 루프 내 2곳) 등이 새로 잡혔다.

**회귀 확인:** L005 2건 65점 그대로. L001만 건드렸음이 확인됨.

## 8. 남은 것

- [ ] 도메인 룰(D***)이 진입점 배수를 안 타는 구조 — D003이 로직 룰과 같은 자에서 경쟁하지 못한다. 신규 섹션으로 우회했으나 근본 해결은 아님
- [ ] 페이지 데이터소스의 XPath는 아직 수집 대상이 아님 (마이크로플로우 Retrieve만)
- [ ] D003 역추적(blame) 미지원 — 도메인 룰 전반의 기존 한계
- [ ] `LoginAsset.Account`의 `UserID`(13곳) 같은 명백한 키 속성은 인덱스 우선순위가 높다. 실제 적용은 Studio Pro 작업
